{
	"name": "inacord",
	"private": true,
	"version": "0.5.11",
	"type": "module",
	"scripts": {
		"dev": "bun --cwd ../nie-web run dev:desktop",
		"build": "bun --cwd ../nie-web run build:desktop",
		"preview": "bun --cwd ../nie-web run preview:desktop",
		"typecheck": "bun --cwd ../nie-web run typecheck:desktop",
		"tauri": "tauri"
	},
	"dependencies": {
		"nie-web": "workspace:*"
	},
	"devDependencies": {
		"@tauri-apps/cli": "catalog:"
	}
}
