{
	"name": "inacord",
	"private": true,
	"version": "1.0.0",
	"type": "module",
	"scripts": {
		"dev": "bun run --cwd ../nie-web dev:desktop",
		"build": "bun run --cwd ../nie-web build:desktop",
		"preview": "bun run --cwd ../nie-web preview:desktop",
		"tauri": "tauri"
	},
	"dependencies": {
		"nie-web": "workspace:*"
	},
	"devDependencies": {
		"@tauri-apps/cli": "catalog:"
	}
}
