{
	"name": "nie-monorepo",
	"version": "0.5.0",
	"description": "Monorepo Bun du dépôt nie — bibliothèques (packages/*) et applications (apps/*). Nommé `nie-monorepo` et non `nie` : ce dernier est le paquet FFI (packages/nie).",
	"type": "module",
	"private": true,
	"workspaces": {
		"packages": [
			"packages/*",
			"apps/*"
		],
		"catalog": {
			"typescript": "5.9.3",
			"@types/bun": "1.3.14"
		},
		"catalogs": {
			"mcp": {
				"@modelcontextprotocol/sdk": "1.30.0",
				"zod": "3.25.76"
			}
		}
	},
	"scripts": {
		"build:ffi": "cargo build -p nie-ffi",
		"build": "bun run --filter '*' build",
		"test": "bun run --filter '*' test",
		"typecheck": "bun run --filter '*' typecheck",
		"lint": "bunx oxlint -c .oxlintrc.json -A style -A pedantic -A restriction packages apps",
		"lint:fix": "bunx oxlint -c .oxlintrc.json -A style -A pedantic -A restriction --fix packages apps"
	},
	"engines": {
		"bun": ">=1.3.0"
	},
	"overrides": {
		"@aphrody-code/bxc": "npm:@aphrody/bxc@^0.6.7",
		"@aphrody-code/zukan": "npm:@aphrody/zukan@^0.1.5"
	}
}
