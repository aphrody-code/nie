[package]
name = "nie-wiki"
description = "Exploration game-data IEVR depuis le miroir SQLite (personnages, skills, items, équipes)"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[lints]
workspace = true

[dependencies]
anyhow.workspace = true
thiserror.workspace = true
serde.workspace = true
serde_json.workspace = true
rusqlite.workspace = true
tracing.workspace = true
redis.workspace = true
rand = { version = "0.8", features = ["small_rng"] }
# NB : interpolation de stats réimplémentée localement (StatBlock 2-segments f64, model.rs/query.rs) —
# divergence VOULUE de nie-core (3-segments f32, le miroir SQLite n'a pas le palier lv30) → pas de dép nie-core.
