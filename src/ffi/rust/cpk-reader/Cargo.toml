[package]
name = "cpk-reader"
version = "0.1.0"
edition = "2021"
description = "Read-only CPK (CRI Package) archive reader — pure Rust, zero C/C++ deps"
license = "MIT"

[lib]
name = "cpk_reader"
path = "src/lib.rs"

[[bin]]
name = "cpk_cli"
path = "src/bin/cpk_cli.rs"

[dependencies]
