# niers — roadmap vers 100 % de portage Rust (pixel-perfect)

> Document maître de la trajectoire vers l'objectif : **rejouer 100 % d'*Inazuma Eleven: Victory Road*
> (IEVR, `nie.exe`, moteur Level-5 « Lives ») en Rust pur, headless + WebAssembly, au pixel et au byte près.**
> Plan opérationnel court : `PLAN.md`. Boucle RE + découvertes : `ARCHITECTURE.md`. Suivi : `jeu-jouable-avancement.md`, `assets-wasm-avancement.md`.
>
> Règle d'or : **aucun « FAIT » sans validation end-to-end sur le réel** (byte-à-byte vs iecode/inagle, pixel-à-pixel vs le jeu). Le repo a déjà connu plusieurs *faux FAIT* (fixtures synthétiques qui passaient, vrais fichiers qui cassaient) — ils sont la dette à ne jamais recréer.

## 1. Ce que « 100 % pixel-perfect » veut dire (décomposition mesurable)

« 100 % » n'est pas un nombre unique. C'est la conjonction de cinq couvertures distinctes, chacune mesurable :

| Couverture | Définition opérationnelle | Métrique | État (mis à jour 2026-06-10) |
|---|---|---|---|
| **C1 Formats** | tout conteneur/asset du jeu est lu nativement en Rust | % des 250 800 fichiers CPK lisibles + décodés correctement | **84,06 %** lisibles ; **audio HCA décode** ✓ (clé IEVR + magic masqué) |
| **C2 Données** | toute famille de config du jeu est portée et recalculée au bit | familles `cfg.bin` portées / 58 existantes | **~77 modules nie-data, 1299 tests** ; **+9 (2026-06-15) : playstyle, unlock_condition, ctrl_chara, chara_details, video_waza, chara_base, text (sanitize_text), chara_description, chara_text** — port 1:1 inagle, golden réel + **validation END-TO-END live sur le vrai VFS**. `chara_text` : noms localisés (20802 NOUN_INFO + variante romanisée), sémantique **last-wins** de `buildNameMap` reproduite (Endou `0xE551FF12` → 3 formes `["Mark Evans","Evans","Mark"]` → prénom « Mark » ; `0xE5655F9E` → « Evans »). **+ résolveur de texte UNIVERSEL `text::parse_text_file`** (port de `text-parser.ts` : `TEXT_INFO`@idx2 / `NOUN_INFO`@idx5, catalogue des **45** fichiers `*_text`, `find_text` last-wins) — débloque la résolution de TOUT texte localisé par hash ; cross-check live : générique == spécifique sur 5568 desc. **+ `event_subtitle`** (cutscenes voicées : parseurs par-fichier Subtitle timecodé / dialogue brut / table washa lip-sync ; port 1:1 inagle) — END-TO-END sur `ev09_05000` : **jointure hash 22/22** subtitle↔dialogue, texte brut préservé (tags `<MNT:…>` conservés). **+ jointures texte via le résolveur universel** (port fidèle des jointures inagle) : `quest::resolve_title` (`titleHash`→`quest_title_text`) — **182/182 titres fr** (ex. « Tu n'as pas perdu le foot ») ; `item::resolve_name`/`resolve_description` (`nameId`/`descId`→`item_text`, descriptions dans la même table) — **1767 noms / 324 descriptions fr** (ex. « Guts Gear », « Barre protéinée ») ; `mission::resolve_name` (`nameId`=var[2]=`raw[0]`→`mission_text`) — mécanisme vérifié sur le vrai `mission_text` (le `mission_config` du jeu est un stub `msa999999`, 0 nom localisé — fidèle à inagle). Le résolveur de texte est désormais exploité (quest/item/mission), pas juste construit. **+ capstone chaîne personnage** : `chara_base::resolve_first_name`/`resolve_last_name`/`resolve_description` (jointures `nameHash`/`lastNameHash`→`chara_text`, `descriptionHash`→`chara_description`) — END-TO-END `chara_base`→`chara_text`→`chara_description` : **6470/7223** prénoms résolus, Endou `c01000010` → « Mark Evans » + sa bio fr complète. **+ `trophy`** (succès) : `resolve_name`/`resolve_description` (`trophy_text`) + `decode_unlock` (compose le décodeur `unlock_condition` sur `open_cond`) — END-TO-END : **231/231 noms** résolus, 108 descriptions, **231 conditions de déblocage décodées** (`{always:142, story:35, eventFlag:54}`) ; 1ʳᵉ jointure composant DEUX briques de la vague (résolveur texte + décodeur unlock). **+ `shop::resolve_name`** (`name_hash`→`shop_text`, modèle documenté inagle) — END-TO-END : **14/16 boutiques** résolues (« Boutique Kizuna », « BB Mart », « Centre commercial chronique », « Boutique VS », « Goal-Marché … »). **+ correctif santé workspace** : test fragile `nie-headless::match_graines_differentes` (assertait que les graines 1 vs 2 divergent, mais le modèle de but grossier les fait collisionner sur « 1-1 » ; la sim EST sensible à la graine — seed 3→0-0, 5→3-2, 42→1-6) réécrit en balayage `1..=10` → ≥2 sorties distinctes. **Workspace entier vert : 2178 tests, 0 échec** (2 tests FLAKY pré-existants corrigés → déterministes : `nie-headless::match_graines_differentes` [balayage de graines] et `nie-formats::mevbin::real_file_golden` [sélection `.mevbin` triée]). **Écart `c000302_p070` ÉLUCIDÉ** (dump records) : en-tête `COUNT=[178,457]` mais **177 records MOT** réels (EVENT 457 = exact) → le parseur est CORRECT, l'en-tête jeu SUR-COMPTE les motions (pas un bug de portage). **+ `aura::resolve_name`/`resolve_description`** (noms d'**Avatar / Keshin / hissatsu** via `skill_text`) — END-TO-END : **443/443 auras** résolues (noms ET descriptions) : « Lancelot, le spadassin héroïque », « Apollon, dieu du soleil », « Musashi, le samouraï vaillant », « Chioné, reine des neiges », « Surt, géant du feu » — le contenu signature d'IEVR. **+ `ai::resolve_strategy_name`/`_description`** (stratégies IA via `ai_text`) — END-TO-END **62/62** (modèle de données CONFIRMÉ purement end-to-end, `ai.rs` n'ayant pas de parseur inagle ; noms internes JA : « 基本戦略 », « オフェンス時基本戦略 »). **+ `belong_team::resolve_team_name`** (noms d'équipe via `team_text`, port inagle) — END-TO-END **208/208** : « Raimon », « Royal Academy », « Inazuma KFT », « Cybertech »… **+ `chara_series`** (nouvelle famille `chara_series_config`, 9 séries + table de noms par type codée en dur ; `chara_base::resolve_series_name_fr`) — END-TO-END : Endou = **« Inazuma Eleven »** (IE1) ; distribution réelle franchise (IE 1058, GO 980, Victory Road 1149…). **La chaîne personnage est 100 % COMPLÈTE** : `chara_base`→ nom (`chara_text`) + bio (`chara_description`) + **équipe** (`belong_team`→`team_text`) + **série** (`chara_series`) + genre ⇒ **Endou `c01000010` = « Mark Evans », équipe « Raimon », série « Inazuma Eleven », bio fr**. Export `characters-resolved/v3` (6470 ; 5866 équipes, 5894 séries). **Leçons anti-faux-FAIT** (jointures NON portées car non validées end-to-end) : `formation_text` ABSENT de cette version ; `skill_technic` = données de **motion** ; **crest d'équipe** (`team_emblem_id_v` → `emblem_resource`) = **0/208** (fichier live = 2 entrées, crest **templé** via `<resourceID>`, pas un lookup) — tous vérifiés AVANT/au-moment du port, revert propre, finding documenté. `playstyle` 6166 ; `unlock_condition` (base64→story/event-flag) 473 0-panic ; `ctrl_chara` 155 ; `chara_details` 550 ; `video_waza` (3 listes RDBN + jointure ; END-TO-END a détecté `fede*Time`=Float) ; `chara_base` (**table maîtresse perso**, 7223 nœuds, Endou `c01000010` golden) ; `text::sanitize_text` (port regex→passes no_std, furigana/balises/échappements) ; `chara_description` (5568 desc nettoyées, 0 balise résiduelle, cross-link `chara_base.description_hash`→texte d'Endou). Méthode : probe live → port → golden inline → assert end-to-end. tree toujours vert. `playstyle` : `playStyle` (6ᵉ var Int de `CHARA_PARAM_INFO`, libellés EN/FR `0..=5`), port 1:1 inagle, **END-TO-END sur les 6166 noeuds du vrai dump** (`{0:1055,1:1034,2:1059,3:1003,4:989,5:1026}`, 0 hors-plage). `unlock_condition` : décodeur des blobs `openCond`/`condition` (gallery/scene_archive) — base64 → arbre story-seuil/event-flag(CRC32) — port 1:1 inagle (+ vecteurs golden de sa suite TS), **END-TO-END : 473 conditions réelles décodées, 0 panic** (story 194 / eventFlag 233 / composite 25 / always 21). Vague précédente +9 (shop, capsule, team, quest, extend_story, nfc, inacode, players_universe, movie). golden réel byte du VFS, tree toujours vert |
| **C3 Logique** | toute fonction de gameplay/moteur reversée est portée et validée | fonctions portées / 52 783 réelles ; masse `.text` portée | **~56 fn = 0,1 %** ; **boucle de match jouable** pilotée par les vraies données ; **PRNG `lives::CRand` (MT19937) porté BYTE-EXACT** (validé vs vecteur réf) — 1er primitif moteur réel |
| **C4 Rendu** | la sortie visuelle est identique au jeu | Δpixel vs capture de référence (PSNR/SSIM) sur scènes-test | **non démarré** (assemblage GLB statique seulement) |
| **C5 RE (échafaudage)** | toute fonction réelle est identifiée (classée ET nommée) | classées + **nommées** / 52 783 | **93,36 % classées** (92,45 → arêtes indirectes) ; **6 429 (12,18 %) nommées** structurellement (0 → vtable-struct) |

Le verrou stratégique : **C5 (RE) est un moyen, pas la fin.** 92 % classé ≠ 92 % compris. Le chemin vers C1–C4
(le jeu jouable) ne dépend pas d'atteindre 100 % de C5 ; il dépend d'avoir **nommé et compris** les fonctions
des sous-systèmes qu'on porte. Donc la priorité bascule de « monter le % de classification » vers
« nommer + porter par sous-système ».

Le chemin **opérationnel** vers le jeu jouable passe désormais par la **GUI native** (crate `nie-game`,
host wgpu, pilier D1/C4) : c'est la **pointe active**. Le pont **azalee** (pilier B′) redevient un
**compagnon web secondaire** (livré, plus le cap). Détail de la stack runtime : `docs/STACK.md` ;
inventaire mesuré par pilier : `docs/INVENTAIRE.md`.

## 2. Théorie de la victoire (pourquoi c'est atteignable, et comment on le prouve)

1. **La vérité terrain existe et est triple** : iecode (C# .NET, formats + engine partiellement reversés),
   inagle (TS, données + formules de stats au bit), et le réel (`.pdata`, dumps `data/`, captures du jeu).
   Chaque portage se valide contre au moins une, idéalement les trois. Rien n'est « supposé ».
2. **`.pdata` borne le problème** : 52 783 fonctions réelles, pas un nombre ouvert. Le dénominateur est fini.
3. **Le RE est récursif et amorçable** : ancres (strings + RTTI) → propagation → nommage → portage →
   nouvelles ancres. Chaque fonction portée devient une ancre de haute confiance pour ses voisines.
4. **La preuve est automatisable** : tests golden byte (formats/données), diff pixel (rendu),
   recoupement `.pdata`/RTTI (RE). La progression est donc **mesurable à chaque commit**, pas déclarative.

## 3. Les cinq piliers — jalons quantifiés et *gates*

### Pilier A — Formats (C1) → 100 % des fichiers lisibles + décodés correctement
- **A0 (FAIT)** : RDBN, g4tx/g4md/g4mg/g4pk, @UTF, CRILAYLA, CPK (clé CRC32), assemblage GLB texturé.
- **A0-corpus (FAIT 2026-06-16)** : robustesse des parseurs **prouvée sur tout le corpus réel** —
  `format_corpus.rs` (game-gated). (1) **12090 `.objbin` + 6927 `.g4pkm` → 100,00 % de succès, 0 panic**
  (19017 fichiers ; plancher 99 %). (2) **cfg.bin (cœur data) : 5855 `gamedata/*.cfg.bin`, LES DEUX chemins → 0 PANIC, 100 %** :
  routage par magic → **T2B 5624/5624** (`parse_t2b`) + **RDBN 231/231** (`parse`) tous parsés. Le
  correctif d'overflow d'en-tête (sign-check + `checked_add`) a résorbé tous les panics ; le
  `catch_unwind` du montage VFS est désormais purement défensif (jamais déclenché sur le réel). Capte les bugs de cas-limite que les
  tests-échantillons ratent. (Complète `menu_setting_corpus_*`.)
- **A1 (N/A pour le PC — vérifié 2026-06-10)** : `nxtch` est la variante texture **Switch** ; **0/250 800** fichiers de l'IEVR PC sont NXTCH (textures = DDS dans g4tx, déjà décodées via `image_dds`). Le code deswizzle reste pour complétude/Switch mais **hors chemin critique** du pixel-perfect PC.
- **A2 (FAIT 2026-06-10 ; généralisé 2026-06-16)** : **HCA décode réellement** via `cridecoder` (clHCA) + clé IEVR `0x00D2997C0DC5EE72` + magic masqué + sous-clé AFS2. **Généralisation validée sur ≥3 AWB RÉELS distincts du VFS Steam** (test `nie-model-serve::hca_decode_generalise_sur_plusieurs_awb_reels`, skip si jeu absent) : `ch.awb` (48 kHz **mono**), `common.awb` (48 kHz **stéréo 2ch** — couverture stéréo que le test mono initial n'avait pas), `d00010000.awb` (mono) — tous décodés `Ok`, non silencieux, `sample_rate`/`channels` plausibles, en 2,5 s (banques par-cue ; streams `bgm`/`anime_stream` exclus pour la rapidité). **La clé Steam = la même** (déchiffrement réussit sur les fichiers Steam). Reste (mineur) : clé PC/Steam alternative éventuelle (patch 1.2.2) si un AWB futur échoue.
- **A3** : `g4sk` hiérarchie d'os sans fallback heuristique. *Gate : arbre d'os exact vs C# sur ≥3 `.g4sk` réels.*
- **A4** : formats résiduels — parseurs réels. *Gate : 100 % des 250 800 fichiers passent en parse sans erreur.* Faits : `p3lip` 20 357 (`lip.rs`), `objbin` 11 920, `mevbin` 328. **`g4nv`/NAVM — FAIT (2026-06-19)** : `nie-formats/src/navm.rs` (port iecode `NavmParser.cs`, magic « NAVM » + en-tête + `section_counts[5]`), **validé byte sur les 156 `.g4nv` réels du VFS : magic NAVM 156/156 + invariant `header_size+data_size==file_size` 156/156** ; golden real-fixtures (`s82g001`={120,102,30,40,51}=réf iecode s28g001b, `w10g030`) + tests synthétiques, clippy 0. **`g4cm`/G4CM (caméra) — FAIT (2026-06-19)** : `nie-formats/src/g4cm.rs` (même convention Level-5 que navm, reversée sans réf iecode), **validé byte sur les 1210 `.g4cm` réels : magic G4CM 1210/1210 + invariant `header_size+data_size==file_size` 1210/1210** ; golden real-fixtures (ev63/ev74 caméras d'événement) + synthétiques, clippy 0. **`dxbc`/DXBC (shaders) — FAIT (2026-06-19)** : `nie-formats/src/dxbc.rs` parse le conteneur **public Microsoft** (`vfxo` vertex 1335, `pfxo` pixel 1113 + cfxo/gfxo) : digest/version/`total_size` + chunks `RDEF`/`ISGN`/`OSGN`/`SHEX`/`STAT`, tout borné. **Validé byte 2448/2448 `.vfxo`+`.pfxo` réels** (magic DXBC + invariant `total_size==file_size` + chunks dans les limites) ; golden + synthétique + rejet-corrompu (chunk_count énorme → `Corrupt`, pas de panic), clippy 0. `fxbin` 372 / `ptlb` 655 — **PARSE PROUVÉ (2026-06-19)** : ce sont des conteneurs **T2B** (footer `0xFFFFFFFF`) → `cfgbin::parse_t2b` les lit (fxbin → `SHADERFX_MEMB_NUM`/`TEC_BGN` ; ptlb → `PARTICLE_NODE_INFO`), golden test `cfgbin::fxbin_et_ptlb_parsent_comme_t2b` + validé live via `/cfg`. **`col`/PXCL (collision) — FAIT (2026-06-19)** : `nie-formats/src/col.rs` — PXCL est un **conteneur Level-5** (même en-tête, `header_size`=0x30, `type_id`=0x65) enveloppant des données PhysX-cooked ; le conteneur est parsé byte-exact (l'intérieur PhysX reste opaque = domaine SDK PhysX, non décodé). **Validé byte 1143/1143 `.col` réels** (magic PXCL + invariant), via `level5`, golden + synthétique, clippy 0. **`g4mt`/G4MT (matériaux/motion) — FAIT (2026-06-19)** : `nie-formats/src/g4mt.rs`, **validé byte 62/63 `.g4mt` réels** (magic G4MT + invariant ; le 63ᵉ renvoie 0 octet via model-serve `/raw` = artefact d'extraction de ce CPK, pas du parseur — qui renvoie `TooShort` proprement). **Helper partagé `nie-formats/src/level5.rs`** : en-tête commun Level-5 (magic/header_size/type_id/align/data_size + invariant) factorisé, utilisé par `navm`+`g4cm`+`g4mt` (DRY). **`g4ma`/G4MA (anim matériau) — FAIT** : `g4ma.rs` (via `level5`), 35/35 réels. Total C1 ce cycle : **4 formats Level-5** (g4nv 156/156, g4cm 1210/1210, g4mt 62/63, g4ma 35/35) + helper `level5` + **conteneur DXBC** (2448/2448) + **fxbin/ptlb prouvés T2B** (1027) → **~3935 fichiers nouvellement couverts/prouvés**. Couverture par extension **98,09 % → ~99,5 %**. Reste : `col` 1143 (PXCL/PhysX, propriétaire) + petite traîne (clobin 39, linb 16, g4tg 9). nie-formats **187 tests verts**, clippy 0.
- **A5** : déchiffrement de toute enveloppe CPK résiduelle si le verrou existe (cf. recherche en cours). *Gate : 100 % des CPK montent dans le VFS.*

### Pilier B — Données (C2) → 58/58 familles portées, recalcul au bit
- **B0 (FAIT 31/58)** : skill, item, growth, exp, passives, aura-cmd, chara-param, formation, command, ai, party, phase, soccer, rpg_battle, mission, dungeon, boost_grp, record, chronicle_top, friendmap, fast_travel, weather, light, dictionary, **+ gallery, banner, search_word, scene_archive, music_app, photo_mode, update_notice, chat_emote, user_name_plate, input** (golden réel byte ; soccer/rpg_battle/event-like = sous-ensembles, contenu restant documenté). 847 tests nie-data.
- **B1 (FAIT)** : INCOMPLET clos — `chara-param` (pairing level-first), `aura-cmd` (whs01780 réel). *Gate tenu : golden byte contre `data/common/gamedata`.*
- **B2 — SATURÉ vs la référence inagle (constaté 2026-06-16)** : diff des **~68 familles de parsers inagle** (`packages/inagle/src/parsers/*.ts`) contre les **90 modules / 1334 tests** nie-data → **toute famille de données qu'inagle parse réellement est couverte**. Les écarts apparents du diff sont des FAUX POSITIFS (nom de module ≠ nom de famille) : `enjoy_mode_team`→`team.rs`, `phase_title`→`phase.rs`, `nfc_lottery`→`nfc.rs`, `passive_skill_effect`→`passive.rs`, `hero`→`soccer_chara_unique_rarity.rs`, `super_tactics_base`→`super_tactics.rs`, `vsroute`/`craft` couverts ; OU des **stubs inagle** (`buildEventDatabase` = « skip complex parsing to avoid noise », rien à porter au sol) ; OU des **agrégateurs génériques** (`buildMenuDatabase` = noms localisés `gamedata/menu/*` = déjà le résolveur de texte universel `text::parse_text_file`). **Aucune famille propre, validable au sol (golden vs inagle/jeu), ne reste non portée.** ⇒ La pointe n'est plus B2 (données) mais **D1 (rendu)** et **C (logique moteur)**, qui exigent de la RE profonde, pas d'autres ports de données. *Gate (historique) : pour chaque famille, round-trip parse + 1 golden réel.* **Cartographie historique du reste (scopée 2026-06-12, désormais résorbée)** :
  - *Petites/moyennes data, lot en cours* : chara_bank, skill_view, post, craft, trophy, setting_menu, vsroute, help.
  - *Grosses single-file portables* : shop (9,5M), capsule (gacha, 6,6M), trophy.
  - *Multi-fichiers à batcher* : quest (66 fichiers, 3M), system (28, 6M), team (4 : team_config/opponent_team/enjoy_mode/rpg_team_name — **nourrit le match, prioritaire**), players_universe, post.
  - *Énormes à subsetter (comme soccer/rpg_battle)* : event (54 fichiers, 272M — scripting/cutscene), character (45 sous-fichiers, 131M — **déjà partiellement couvert** : chara_param/exp/growth/passives en sont issus ; le reste est surtout config de **rendu** chara_model/mesh/texture/uniform → relève plutôt de C4).
  - *Non-data (assets/système, hors C2-données)* : font, motion, movie, live2d, staffroll, debug, common, data_file, nfc, w17, inacode, menu (rendu). Le dénominateur « 58 » est donc large ; ~40 sont de vraies familles de données.
- **B3** : moteur de calcul dérivé (stats finales, formations, bonus d'équipe) recoupé inagle au bit.
  - **Stats finales — FAIT (confirmé 2026-06-16)** : `nie-data/src/growth.rs` (`calculate_stats` : interpolation Lv1→30→50→99, `rarity_to_growth_rank`, lookups à fallback ; port 1:1 inagle `stat-calculator.ts`) **recoupé au bit contre le VRAI `calculateStats` inagle sur tables réelles** (`growth_golden.rs` : Lv1/15/30/40/50/75/99 d'un set de params réel = exacts). Doublé dans `nie-core/growth.rs` + intégré au match (`from_chara_params_and_levels`, C1). *(Tentative de re-port ce cycle → écartée : doublon vérifié, reverté — discipline anti-faux-FAIT.)* Restent : **formations** (placement byte-exact FAIT via `with_formation`, C1) ; **bonus d'équipe** — **finding 2026-06-16 : PAS de calculateur de référence** (inagle `boost-player-group-config.ts` ne fait que PARSER, aucune `export function` de calcul ; niers `boost_grp.rs` aligné = parseur seul). L'APPLICATION du bonus (composition d'équipe → groupes actifs → bonus de stats) est un concern **moteur de match** (runtime C++), pas un port data validable contre inagle → relève de C (RE moteur), pas de B3. Le data de boost-groups EST parsé (FAIT).

### Pilier B′ — Pont azalee : compagnon web secondaire (livré — n'est plus le cap)
> **Reprioritisé 2026-06-13** : valeur réelle livrée (route `/typed` 37 familles, `export_formations`/`export_passives`, explorateur CPK refondu), mais le **cap opérationnel est désormais D1 natif** (`nie-game`/wgpu). Ce pilier reste maintenu comme compagnon, pas comme tête de pont.

**Rappel de cap (2026-06-12)** : porter une famille avec golden ne sert à rien tant que la donnée
n'atteint pas **azalee** (`rg/apps/azalee`, proxifiée `cdn.rosegriffon.fr`). azalee lit le **miroir
SQLite/Supabase** (67 tables `inagle_*`, peuplées par le pipeline TS inagle) + quelques **JSON plats**
dans `apps/azalee/data/` (modèle `nie-data/bin/export_passives.rs` → `passives-full.json`, lu par `/passive`).
niers apporte une valeur UNIQUE sur trois leviers :
- **B′1 — Combler les trous du miroir** : tables vides/maigres qu'inagle n'a pas peuplées mais que
  niers a portées byte-exact. Cibles identifiées : `inagle_formations = 0` (← `formation`, 115
  formations portées), `inagle_missions = 1` (← `mission`), `inagle_drops_treasures = 0`.
- **B′2 — Remplacer les approximations codées en dur** : ex. `azalee/lib/formations.ts` contient des
  positions **estimées à l'œil depuis du CSS de zukan.inazuma.jp** → remplacer par les `start_pos`/
  `offense_pos`/`defense_pos` f32 **réels du jeu** (déjà décodés dans `formation.rs`).
- **B′3 — Industrialiser l'export** : un binaire `export_<famille>` (std+serde) par famille livrable →
  JSON schéma stable dans `apps/azalee/data/`, consommé par la page correspondante. *Gate : le JSON
  généré se charge dans azalee et la page rend la vraie donnée.*

**LIVRÉ (2026-06-13) — pont générique `/typed` + explorateur CPK refondu.** Plutôt qu'un binaire
d'export par famille, la route `nie-model-serve GET /typed/<vfs>.json` décode N'IMPORTE quel cfg.bin
live en structures typées nie-data (dispatch par nom de fichier, **37 familles**, RDBN-`lists` +
T2B-`entries`). Vérifié byte-exact live : formation (115, coords f32), mission (`msa999999`), aura
(387), item (4153), skill (1001), chara_param (6148). Détails dans `nie-model-serve/src/main.rs`
(`cfgbin_to_iecode_root`, `cfgbin_to_t2b_iecode_root` qui réplique le suffixe `<base>_<i>` d'iecode,
`typed_decode`) ; `/cfg` rendu lossless (Blob -> hex MAJ). **nginx** : bloc `location ^~ /typed/`
ajouté à `/etc/nginx/conf.d/cdn.rosegriffon.conf` (proxy 8790, **hors git** — re-vérifier après tout
redéploiement nginx). **azalee** : explorateur `/cpk` refondu en vrais viewers
(`app/cpk/Cpk{ConfigViewer,FormationViewer,JsonTree,HexViewer}.tsx` + `lib/cpk/shared.ts::cpkTypedUrl`) ;
`CpkFormationViewer` = terrain interactif 11 joueurs aux coords f32 réelles → **remplace B′2**
(positions CSS codées en dur de `lib/formations.ts`). Lien modèle `/model` mort -> `/model-full` réel.

**B′2/B′3 LIVRÉ (2026-06-13)** : binaire `nie-data/bin/export_formations` -> `apps/azalee/data/formations-full.json`
(115 formations, **83 valides** = 11 joueurs/1 GK ; label DF-MF-FW dérivé de `position_id` 1..10 ;
positions f32 start/offense/defense). `azalee/lib/formations.ts` expose `GAME_FORMATIONS` (83 vraies,
mappées en top/left %) en plus des 8 legacy (compat des `id` persistés) → le **My Team builder**
(`/tools/my-team`) propose désormais les vraies formations du jeu. C'est le 2e export industrialisé
après `export_passives`.
**B′3 — exports résolus (2026-06-15)** : `nie-game/examples/export_characters` (roster `v2`, **6470
personnages** — prénom/nom + **équipe** (5866/6470) + bio ; Endou `c01000010` = « Mark Evans », équipe
« Raimon », bio fr) et `export_auras` (**base Avatar/Keshin, 443 entrées** :
nom + description + élément, ex. « Apollon, dieu du soleil » / Fire) lisent le jeu **live** (VFS),
résolvent via les jointures de hash de `nie-data`, écrivent un JSON schéma stable (`*-resolved/v1`).
`export_items` (**1767 objets** : catégorie + nom + description + prix/stats) et `export_trophies`
(**231 succès** : nom + description + **condition de déblocage décodée** story/event-flag) et
`export_quests` (**182 quêtes**), `export_teams` (**208 équipes** : Raimon…) et `export_shops`
(**14 boutiques** + inventaires → réfs vers la base d'objets). **Sept** artefacts de **base de jeu
résolue** (personnages / équipes / Avatar-Keshin / objets / succès / quêtes / boutiques) avec
**références croisées** (perso→équipe, boutique→objets), issus de la vague de résolution de texte, lus
**live** du VFS, schémas stables (`*-resolved/vN`) consommables par azalee/wiki — tout le contenu majeur.
Reste B′1 : peupler les **tables miroir** `inagle_*` (lecture serveur d'azalee) ; `/typed` couvre la
lecture live mais pas l'écriture miroir/SSG. Mapping pages↔familles : `/gallery`↔gallery,
`/succes`↔trophy, `/quete`↔quest, `/capsule`↔capsule, `/boutique`↔shop, `/passive`↔passives (FAIT),
`/equipe`↔formation+team.

### Pilier C — Logique moteur (C3) → résoudre la longue traîne par sous-système
- **⚠ VERROU STRUCTUREL C3 — le problème d'oracle (clarifié 2026-06-19)** : les piliers C1/C2 (formats/données) ont un **oracle de validation** (iecode/inagle/cfg.bin réel) → port FAIT byte-exact possible. Le **modèle de but runtime** (détection but, arrêt gardien, agrégation score, physique balle) n'en a **AUCUN** : pas de C décompilé avec vecteurs de référence (les `.c` dispo donnent ctor/struct, pas l'algo), inagle ne couvre que les **données** pas le runtime, aucune capture frame-par-frame. On peut **reverser** ces algorithmes (documentable, anchors localisés cf. `c3-modele-but-anchors-binaire-courant`) mais **PAS les porter en FAIT *validé*** sans le jeu qui tourne → c'est **pourquoi `match_sim` reste nominal** (un port non validé serait un faux-FAIT, interdit). **Voie de sortie** : (a) **differential testing** contre le jeu réel (lancer nie.exe instrumenté + comparer les sorties d'un match scripté), ou (b) reverser+porter en **marquant explicitement « reversé, non validé runtime »** (statut distinct de FAIT). Tant que (a) n'est pas outillé, C3-runtime plafonne à la **reconnaissance** (faite end-to-end) + ports validables seulement (structures/FSM/ball-kinds/params — faits). **Faisabilité de (a) sur ce VPS (vérifié 2026-06-19)** : l'infra EST présente — Wine 10.0, GPU (`/dev/dri/card0` + Vulkan), 45 Go/12 cœurs, exe **EAC-patché** (anti-cheat retiré = principal bloqueur Wine levé). L'oracle n'est donc PAS bloqué par l'infra mais par l'**effort de tooling** : (1) faire lancer le jeu headless (EOS/Steam stub + DX11→DXVK + display virtuel), (2) scripter un match déterministe, (3) instrumenter les sorties (score/événements/positions balle) → comparer au port Rust. Multi-session, mais scopé et faisable ici. (Lancement complet NON tenté : risque hang/spawn sur le VPS de prod.)
- **C0 (FAIT, îlots)** : nie-core (FSM match, effets, action-ctrl, stats — 126 tests) ; nie-engine (~55 fn, 11 modules) **mais 434 `// EXTERN:` non portées** = îlots non connectés.
- **C1 (FAIT 2026-06-10)** : **boucle de match jouable déterministe** (`nie-headless match`) — FSM + horloge + score câblés ; séquence FSM + `final_score` confirmées byte vs C. **Désormais pilotée par les vraies données** : `TeamSetup::with_formation` place les 11 joueurs aux positions byte-exactes du dump `formation_config`, `from_chara_params_and_levels` dérive leurs stats réelles via les tables de croissance (mapping position GK=1/FW=2/MF=3/DF=4 tranché par iecode `types.h:28`). Restent nominaux : modèle de but, PRNG, pondération d'agrégation, `chara_rank` (le réel = moteur physique). 172 tests nie-core.
- **C2** : **résorber les EXTERN par sous-système** — choisir un sous-système (ex. `chara` 11 358 fn, ou `audio` runtime), le nommer (cf. pilier E), porter ses fonctions racines + leurs callees jusqu'à 0 EXTERN dans le module. *Gate : module sans EXTERN, tests par fonction.*
- **C2-match (FAIT partiel 2026-06-19)** : port des **constantes structurelles de l'init de match** (`soccer_play_cmd_manager.c`) → `nie-core/src/play_cmd_manager.rs` : tailles des sous-systèmes (PlayCmdManager 0xA0, event-system 0x1BC0…), params d'init (file `{8, 0x16}`, effets `8`), **objet de scoring byte-exact** (multiplicateur `1.0f`=`0x3F800000`, flags `0x100`, 4 scores), et **structure du système d'événements de match : 9 callbacks** (dont on_goal/on_foul *inférés*) — c'est exactement le **système d'événements dont dépend le vrai modèle de but** (cf. C1bis). Confirmé/inféré distingué, VAs `FUN_*` non reproduites (dérive binaire). 2 tests byte-exacts, clippy 0. **Reste** : reverser les corps des 9 callbacks (FUN_1414ca300 on_goal présumé, etc.) dans le binaire COURANT → reconstruire la résolution tir→but/arrêt (avec `keeper.rs` déjà porté). **Sous-système LOCALISÉ dans le binaire courant (2026-06-19, via `niers.sqlite` RTTI)** : les classes du modèle de but sont identifiées avec leurs VAs courantes — `CSoccerEventCheckComponent` (détection événement/but, vtable COL `0x14183B238` → vmethod_0 `0x140379270`), `SoccerShootCatchInfoMenu` (résolution tir/arrêt, méthodes `0x141043xxx`), `SoccerCalcKeeperSaveComponent` (porté), `SoccerBattleEventPlayEventInfo`. Layout vtable MSVC vérifié (COL au `vtable_vaddr`, méthodes à `+8`) via contrôle keeper. Ce sont les **anchors concrets** pour la RE du modèle de but (cf. mémoire `c3-modele-but-anchors-binaire-courant`). Reversal des corps = multi-session. *Les ports d'algorithmes match propres (FSM/effets/action/keeper/ctrl/tactics) sont faits ; restent les soccer d'orchestration/init (structurels) + le système d'événements (RE profonde).*
- **C1bis (FAIT 2026-06-10)** : **PRNG `lives::CRand` porté BYTE-EXACT** (`crate::crand`, MT19937 32-bit) — décompilé via Ghidra (`docs/recherche-modele-match-decompile.md`), validé contre le vecteur de référence MT19937 (graine 5489). Remplace le Splitmix64 nominal. **Découverte structurante** : la vraie résolution de match n'est PAS une formule inline mais **event-driven (IDs hachés) + data-driven (cfg.bin)** → le modèle de but de `match_sim` reste nominal, à reconstruire depuis le système d'événements (`FUN_1412C0970`, prochaine cible RE).
- **C3** : itérer C2 sur menu / physics(PhysX) / network / script jusqu'à couverture fonctionnelle.

### Pilier D — Rendu (C4) → pixel-perfect — **LA POINTE ACTIVE**

> **Conception détaillée des 2 écrans tête-de-pont (`title02`=start.png, `mainmenu01`=menu.png) :
> `docs/DESIGN.md`** — décomposition élément-par-élément vérifiée adversarialement, 6 couches
> manquantes identifiées (placement, texture/atlas, runtime Lua, texte/police, 3D in-menu, blend/gate),
> et le sous-plan **D1.a–D1.f** ci-dessous. Le bump wgpu/retarget D3D11 (D1 historique) reste requis
> mais le chemin le plus court vers une 1ʳᵉ preuve SSIM passe par ces couches sur `title02` (statique-dominant).

- **D0 (FAIT partiel)** : assemblage GLB statique (corps+face+uniforme texturés), servi par nie-model-serve, **ET rendu par le host natif `crates/nie-game`** (wgpu 22 + winit 0.30, modes `--capture` PNG headless / `--window`, rend une vraie texture `.g4tx` décodée RGBA8, readback aligné 256 o **déjà bit-exact**, 1 180 LOC).
- **D1.a — Placement ancêtre-fallback (FAIT 2026-06-14)** : `nie-formats/src/g4pkm_motion.rs` (`motion_final_pose` + `find_placement_bone_index`, port de iecode `G4pkmMotion.cs:84-192`) branché dans `menu.rs::place_on_canvas`. **PAS un interpolateur de keyframes** (les slide-in n'ont pas de keyframes dans les fichiers, RE iecode) : fallback vers le 1ᵉʳ bone ancêtre on-écran en conservant la scale du candidat. **Effet vérifié** : `--menu title02` passe d'un canvas quasi vide (45 Ko) au logo IEVR rendu **on-écran** (543 Ko). 7 tests motion + 141 tests lib nie-formats verts, clippy 0. Reste (position finale exacte = état runtime C++, hors fichiers) documenté §6 DESIGN.md.
- **D1.b — Sélection texture (FAIT 2026-06-14)** : `g4tx::select_main_texture` (nom == basename du conteneur, sinon plus grande texture DDS non-dummy ; port de la résolution-par-nom iecode `MenuPngService.DecodeNamedTexture`) remplace le `find(|t| t.is_dds)` qui piochait le **dummy 4×4** de tête (`main.rs:1263`). **Effet vérifié** : `--menu title02` rend le vrai logo « INAZUMA ELEVEN Victory Road » (plus le dummy invisible). 3 tests + clippy 0. **Reste (D1.b-bis)** : crop de sous-région d'atlas (UV) pour les icônes atlas-packées (`g4tx` régions déjà parsées) — requis surtout pour mainmenu (D1.c-era).
- **D1.c-texture — Textures co-localisées mainmenu01 (FAIT 2026-06-16)** : diagnostic conclusif du rendu **BLANC** de `mainmenu01` (SSIM 0.0011 vs `menu.png`) — les **31 objbin n'ont AUCUN param `Texture`** (`g4tx_path=None`). Scission : **Groupe A** (8 objbin runtime, sans assets statiques) vs **Groupe B** (23 objbin `_06..19`) dont le g4tx **EXISTE, co-localisé avec le mesh** (`common/.../X.g4pkm` ↔ `dx11/.../X.g4tx` ; chemin g4pkm AUTORITAIRE — objbin `_07` → mesh `_07c`). **Mécanisme confirmé = convention co-localisée** (le g4md de menu n'a pas de `material_base_names`, vérifié vide → ni matériau ni Lua `SetSprite`, hypothèses écartées). `build_sprite_list` + `collect_layout_objects` dérivent `<mesh-stem>.g4tx` quand pas de param `Texture` → **22 sprites de textures réelles rendus (était 0)** ; export layout 23 sprites. **title02 inchangé 0.2511 (0 régression)**, nie-game clippy 0, gate `mainmenu01_ssim_vs_reference` (plancher 0.003). **iecode a la MÊME limite** (`MenuLayoutExporter.cs:127` gate sur `obj.G4txPath`) → rend mainmenu01 vide ; **niers le dépasse.** Reste : **placement** (Groupe B = widgets à bind pose hors-écran → driver C++/Lua **D1.c-driver**, pas de keyframes de position en fichier, cf. §6) + raffinement **uv0** (D1.b-bis). Cf. DESIGN §5.
- **D1.b-det — Déterminisme du rendu (FAIT 2026-06-14, critique)** : le rendu était **non déterministe** (3 runs → 3 PNG différents) car `Vfs.index`=HashMap → `resolve_vfs_basename`/collecte objbin piochaient locale+ordre au hasard. Corrigé : `resolve_vfs_basename` trie+priorise la locale (ordre iecode locale→non-localisé→common→en, `MENU_LOCALE="fr"`), `obj_paths` trié. **Vérifié 3 runs octet-identiques.** Prérequis absolu du gate D1.f.
- **D1.c — Émuler le DRIVER de menu (NON_FAIT, sous-projet — plan révisé)** : découverte (probe `nie-lua/examples/probe_menu_script.rs`) que `OnOpenLayer(layerId)` seul **ne peuple rien** ; le menu est construit par un driver riche piloté par le moteur C++ (`title_menu_2` : `OnInit`, `Step`, `SetupItemButton`, `GetItemButtonNum`, `IsEnableMenuItemButton`, `GetTextureNameItemButton`, `GetVictoryCountNum`, gating DLC/save…). D1.c = **émuler cette boucle** (appeler OnInit → énumérer items → setup/gate/texture/texte par item → compteurs → appliquer `funcLuaMenuCommand` sur `MenuState`) + brancher dans `build_sprite_list`. Séquence d'appels = celle du moteur (RE `nie.exe`/iecode `LuaRuntime`) ; tables `funclua-cmdids.json`/`hash-dictionary.json` absentes du checkout → à rapatrier. Détail : `docs/DESIGN.md` §13.
- **D1.d — Texte + police (AMORCÉ 2026-06-15 : résolution de texte STATIQUE FAITE ; police NON_FAIT)** : **`menu_text` positivement identifié** comme source des libellés UI statiques + **résolu via le résolveur universel `nie_data::text::parse_text_file`** (cf. DESIGN.md §7). Câblé dans l'**export de layout** (`collect_layout_objects` : `TextComponent.hashes` → `find_text(menu_text_fr)`) : `--menu mainmenu01 --export-layout` rend **7 libellés réels** (« Sauvegarder », « Suivant », « Personnages disponibles », « Bonus d'équipe »…) ; title02 = 1 (« Informations », bannière masquée). `nie-data` promu dépendance régulière de `nie-game`. Test gardé `menu_render_gate::mainmenu_static_text_resolves_in_export`. **RÉSOLUTION DE RÉGION PAR HASH — FAIT (2026-06-19, D1.b)** : `--compose-layout` résout le `spriteRegionHash` non résolu en nom = **CRC32(nom de région)** (même mécanisme validé que [`nie-core::ecs`]/`cfgbin::crc32`) en cherchant dans l'atlas de l'objet la sous-texture dont `CRC32(nom)==hash`, et croppe **cette région** au lieu de l'atlas entier (toutes sous-textures empilées). Vérifié : main_menu résout 1 région par hash (`mainmenu90_02_header_tab`→`mode_base02`) **sans régression** (gate SSIM vert), correction **applicable à tous les écrans** ; delta SSIM main_menu petit (1 objet) mais bénéfice = correction générale du compositeur. **RENDU DE TEXTE — FAIT + VÉRIFIÉ SUR LE RÉEL (2026-06-19, 1ᵉʳ vrai progrès C4)** : le pipeline glyphes fonctionne end-to-end. (1) **Métriques** : `font.cfg.bin` (T2B, `font::parse_metrics`) → **7469 glyphes** ; le `.g4tg` était un faux-bloqueur (aucun `.g4tg` de police n'existe, cf. STACK.md). (2) **Atlas** : `decode_texture_rgba` étendu aux **DDS LEGACY non-compressés BGRA8** (vrai bug corrigé — `font_def/font.g4tx` 4096×2048 est legacy BGRA8, masks R=0xFF0000 ⇒ swizzle BGRA→RGBA ; ne décodait pas avant). (3) **Rendu** : `font::draw_text` blit les glyphes ; diagnostic `nie-game --render-text <str> --capture`. **TOUTES les cibles du gate D1.d RENDUES + vérifiées** : « 212 » (3/3 codepoints, 3 clusters de glyphes distincts, les deux « 2 » à profil identique — vérif histogramme), « 99 » (2/2), « ver 6.0.2 » (9/9), « COMMENCER » (9/9, majuscules Latin), + label menu réel « Sauvegarder » (11/11). Majuscules/minuscules Latin + chiffres + espace tous résolus dans la table de 7469 glyphes. clippy 0. **Branchement compositeur — FAIT (2026-06-19)** : `--compose-layout` pose désormais les **libellés RÉSOLUS** au transform de l'objet (passe texte additive, `load_menu_font` + `resolved_text_label` + `font::draw_text` → `DrawItem` au-dessus des sprites). Vérifié : compose `main_menu` rend 1 libellé résolu sans régression (les 2 gates SSIM compose `mainmenu_runtime_compose_ssim`/`shop_runtime_composes_to_image` restent verts), compositeur **text-capable**. **Payoff actuel limité par le driver D1.c** (mesuré honnête) : sur main_menu, 1 seul `text` est un label résolu (les autres sont des **hashes non-résolus** que seul le driver résout) + positionné **hors-canvas** (y=2389, transforms driver non normalisés) → clippé. ⟹ Le pipeline texte est **complet** (rendu vérifié + compositeur câblé) ; sa contribution SSIM est gated par le driver (résolution des hashes + positions on-canvas). **Reste** : kerning (`KERN`), gaiji (pictos), et les apports driver D1.c (résoudre les `text`-hashes + normaliser les positions). Les libellés **runtime** (« COMMENCER ») dépendent du driver D1.c.
- **D1.e — Modèles 3D in-menu (NON_FAIT)** : router objbin `g4tx=None & g4pkm≠None` → passe 3D offscreen (assemble.rs) → vignette AVATAR / VOTRE ÉQUIPE.
- **D1.f — GATE pixel (HARNAIS FAIT 2026-06-14 ; cible SSIM en cours)** : `crates/nie-game/tests/menu_render_gate.rs` — (1) **gate déterminisme** (2 rendus octet-identiques, *hard gate* VERT) ; (2) **SSIM maison** (fenêtres 8×8 luma, 0 dépendance) vs réf jeu `start.png` **2560×1440** downscalée ×½. **Baselines mesurées** : title02 = **0,25** (fond = scène 3D, plafond textures-menu) ; mainmenu01 voie-préfixe = **0,004** (quasi vide) ; **`main_menu` voie-`menu_setting` = 0,418** (2026-06-16, le plus haut — fond plein écran + en-tête composés depuis la liste de layers). Planchers de non-régression sous chaque baseline, **à relever à chaque vague**. Reste : blend additif par `draw_type==1` (2ᵉ pipeline `One/One`), placement exact des widgets (driver), masques ROI, atteindre **≥ 0,99** (dépend de D1.c-driver/d/e). *Gate cible : SSIM ≥ 0,99 hors ROI sur `main_menu` (via setting) puis title02.*
- **D1.c — table cmdId reversée + dispatch porté (FAIT partiel 2026-06-14)** : workflow RE → handler `funcLuaMenuCommand 0x140C91B30` → dispatcher `0x140C8CC00` → **table 1109 descripteurs** `{u64 handler, u32 cmdId, u32 pad}` (file offset `0x1BDE390`). **Table complète extraite** → `data/re/funclua-cmdids.json` (artefact RE recouvré, absent d'iecode). **12 cmdId reversés** (setters visible/sprite/text/color/layer-active/part-visible + 6 getters), **layouts d'args corrigés** (PAS de layerId universel en pos 1 — bug iecode). **Porté** dans `nie-lua/menu_host.rs` (dispatch table-driven, `current_layer`, `command_name()`) → `OnInit` de `title_menu_2` : **`known=7 unknown=0`** (était 0/7). 5 tests dispatch.
- **D1.c-driver — séquence reversée (FAIT 2026-06-15) ; population BLOQUÉE sur données de scène** : manager `0x14109D190` reversé (haute confiance) — énumère les layers depuis la SCÈNE `[mgr+0x130]` (array@+0x88, count@+0x90, stride 0x18, `{name, layerId, valid}`), charge `menu/<name>.lua.bin`, pose `__menuObjPtr`, `OnInit()` (0 arg), puis dispatchers `OnSetupLayer(layerId)`/`OnOpenLayer`/`Step`. **Vérifié : la séquence seule crée 0 objet** car `GetItemButtonNum` lit le nb d'items depuis l'**état C++ scène/save** via les natives funcLua. Pixel-perfect menu = **sous-système data-driven** : (a) **parseur de SCÈNE**, (b) **natives funcLua lisant l'état niers**, (c) couche **save/progression**. Pseudocode+adresses : `docs/DESIGN.md` §13. **(a) parseur de SCÈNE — FAIT 2026-06-16** : la SCÈNE = liste de layers EST data-driven via **`*_menu_setting.cfg.bin`** (485 fichiers, un par écran, `gamedata/menu/cfg/`), liste `MENU_LAYER_INFO_LIST_BEG > MENU_LAYER_INFO*` = `{layer_id, name, objbin_path, params}`. Porté `nie-data/src/menu_setting.rs` (RE originale — pas de parseur inagle), **validé end-to-end** : `layer_id == CRC32(name)` sur **4 écrans réels** (`main_menu` 13 layers, `victory_road_main_menu` 7, `title_menu_2` 32, `capsule` 17 — **0 mismatch**). `tests/menu_setting_golden.rs` (4 tests, fixture réelle hermétique + auto-validation CRC32). **Étendu : `MENU_CMD_INFO`** (commandes liées aux layers — `CMD_FCS_BACK`/`CMD_FCS_NEXT`/`CMD_FUNCTION` que le driver dispatche via funcLua) **+ `MENU_LAYER_GROUP_BASE`** (drapeaux par layer : `flag0=1` marque le **layer interactif**, `MenuSetting::interactive_layer_id`) → fondation data de la **brique (b)** (interactivité). **Validé end-to-end + cohérence cross-liste** : 4 commandes + 13 layer-groups sur le vrai `main_menu`, et le layer interactif (flag0=1, unique : `0xBF14058` header_tab_icon) EST exactement celui qui porte les commandes. 5 golden tests. **+ VALIDATION CORPUS** (`menu_setting_corpus_layer_id_is_crc32`, game-gated) : les **467** `*_menu_setting.cfg.bin` du VFS parsés → **0 panic, 4657 layers, 4716 commandes, 0 mismatch `layer_id==CRC32(name)`** → parseur robuste + correct sur TOUT le corpus, pas seulement les 4 écrans golden. **Découverte structurante** : un écran compose des layers de **plusieurs préfixes d'objbin** (`main_menu` = `mainmenu90_*` fond/en-tête + `cmn01_*` icônes + `mainmenu01_*` guides + `rpg00_*`) → le filtre par préfixe de nom de `build_sprite_list` ne voit qu'une PARTIE de l'écran ; **la composition correcte se lit dans le menu_setting.** (Le `victory_road_main_menu_setting` 7-layers et le `main_menu_setting` 13-layers coexistent — variantes de mode.) **Reste pour le driver** : (b) natives funcLua lisant l'état niers (compteurs d'items, gating), (c) save/progression — toujours du RE C++/Lua. Mais (a) est désormais une vraie brique data validée. **CÂBLÉ DANS LE RENDU + L'EXPORT AZALEE + GATE (2026-06-16)** : `--from-setting` compose l'écran depuis la liste `MENU_LAYER_INFO` via le helper partagé `setting_objbin_paths`, branché dans LES DEUX chemins — rendu PNG (`build_sprite_list_from_setting`) ET export layout azalee (`collect_layout_objects`/`cmd_export_layout`, schéma `MenuLayout`). `--menu main_menu --from-setting --export-layout` produit les **13 objets RÉELS** (multi-préfixes : `mainmenu90`×5/`mainmenu01`×4/`cmn01`×3/`rpg00`×1, 13 sprites) au lieu des 31 `mainmenu01_*` partiels. Rendu PNG → **13 layers, dont le FOND plein écran `mainmenu90_00` (2640×1100, pose on-écran) + l'en-tête**. **SSIM main_menu vs `menu.png` = 0,418** (gate `mainmenu_via_setting_ssim_vs_reference`) — **×110 vs la voie préfixe `mainmenu01_*` (0,004, quasi vide) et > title02 (0,251)**. Nouveau **haut-fait du rendu de menu**, et preuve que mainmenu est atteignable en **2D** (pas de plafond scène-3D comme title02). Reste à monter : placement exact des widgets animés (driver b/c) + blend/atlas-UV. **Finding placement (2026-06-16, mesuré + écarté)** : raffiner `find_placement_bone_index` pour préférer le 1ᵉʳ bone « base » NON-trivial (sauter `_pos_base01` origine→racine, prendre `_header_base01` plein-largeur) place mieux l'en-tête/onglets `main_menu` (**0,418→0,446**) MAIS régresse `title02` (**0,251→0,245**) → **PAS universel**, c'est du placement spécifique-driver, pas l'heuristique partagée iecode. Reverté (fidélité title02). Confirme : le placement exact relève du driver (b/c), pas d'un tweak d'heuristique. **BRIQUE (b) PARTIELLEMENT VIVANTE (constaté + verrouillé par gate 2026-06-16)** : `--menu main_menu --runtime` exécute le VRAI `main_menu_1.lua.bin` (top_level_ok, `OnInit` OK, `OnOpenLayer` OK) et **`enumerate_header_tabs` énumère les 9 ONGLETS de navigation réels** (types 10/20/30/40/50/60/70/80/90, via `GetSortOfTabs`/`GetMenuObjectNameFromTabType` du script) → 9 objets `mainmenu_header_tab_<type>` avec leur libellé. Gate END-TO-END `mainmenu_runtime_driver_enumerates_9_header_tabs` (sur le vrai script, plus seulement le stub unit-testé). **Donc le driver extrait DÉJÀ une partie de la navigation runtime.** Restent précisément : (i) **RENDU** des onglets (résoudre `obj_hash`→objbin→texture + placement `SetupHeaderTab`) ; (ii) **item-buttons** (`GetItemButtonNum` lit l'état scène/save C++ → 0 pour l'instant) ; (iii) **1 commande funcLua inconnue** dans `OnInit` de main_menu — **REVERSÉE + PORTÉE ce cycle (2026-06-16)** : cmdId `0x65E825B1` → handler `0x140CBF150`, **désassemblé** (`r2` sur le vrai `nie.exe`) : lit un octet de config global `[[0x1421107A8]+0x69C8]+0x2CAA1E`, l'applique via `0x1405CF860(ctx,byte)`, puis **renvoie AL=1 inconditionnellement**. Appelé sans argument. niers ne réplique pas la mutation d'état moteur, mais le **retour CORRECT est `1`** (le défaut getter `0` aurait été FAUX si le script teste le retour) → porté `CMD_APPLY_GLOBAL_CONFIG_TRUE` dans `menu_host` (renvoie `1.0`), 2 tests unitaires (command_name + retour). **Effet : `OnInit` de main_menu passe de `known=0 unknown=1` à `known=1 unknown=0`** — couverture funcLua de main_menu COMPLÈTE (comme title_menu_2). **C'est le 1ᵉʳ d'une FAMILLE de 3 cmdId reversés from-scratch ce cycle** (les 12 ports antérieurs venaient de RE préalable). Famille « apply → return true » (no-arg) : **`0x65E825B1`** (h. 0x140CBF150, main_menu), **`0xC3135B00`** (h. 0x140CEEE80, shop — `mov al,1` inconditionnel), **`0xB9FFF3C9`** (h. 0x140C96A50, title02 — branche flag global `[0x141D842F0]`, cas par défaut/état-frais renvoie 1). Tous renvoient `1.0` (le défaut getter `0` était FAUX). Effets : main_menu `unknown 1→0`, title02 `5→4`, shop `27→26`. **+ diagnostic** : `runtimeSummary.unknownCmds[].args` surface les args (aide la RE). **BACKLOG identifié (scan runtime 2026-06-16)** : **30 cmdId distincts encore inconnus** sur title02 (5)/shop (27) — args capturés. Beaucoup sont des **setters multi-args complexes** (ex. `0x16C1C4C0` h. 0x140CD8E30, 4 args, écritures de champs moteur — pas un quick-win) ; les no-arg « apply→true » sont les quick-wins (3 faits). La brique (b) avance désormais **un handler à la fois par désassemblage `r2`/nie.exe** — méthode rodée + reproductible. **DÉBLOCAGE ITEM-COUNT (2026-06-16, 4ᵉ cmdId reversé ce cycle) : `0x16C1C4C0` (handler `0x140CD8E30`)** = `RegisterItemListCount(objId, hash, _, count)`. **CORRECTION MAJEURE de l'analyse** : le désassemblage prouve que ce setter et `GetObjectAttr` (handler `0x140CF4F90`) partagent **le MÊME manager `0x140CF5B60`** → le **nombre d'items vient du SCRIPT** (qui appelle `0x16C1C4C0`), PAS (seulement) du save-state C++ comme supposé. Confirmé : args réels `(2250456639,…,8)` == golden `object_attr[2250456639]=8`. Porté `object_attr[objId]=count` → **le loop `SetupItemButton` du script S'EXÉCUTE désormais** (avant : bloqué car `GetItemButtonNum`=0) : title02 `MenuState objets 22, listItems=1` ; shop `listItems=1`. Les nouveaux unknowns révélés (title02 4→7) sont les commandes `SetupItemButton` du loop — **prochaines cibles de reversal**. ⇒ Le blocage « item-buttons » n'est PAS purement save-state ; il se résout en portant la chaîne de commandes d'enregistrement d'items (reversal incrémental, méthode prouvée). **Total ce cycle : 4 cmdId reversés+portés from-scratch** (3 apply→true + RegisterItemListCount), nie-lua 32 tests. **Effet VERROUILLÉ par gate** `title02_runtime_driver_builds_item_content` : sur `title02`, le runtime driver crée du contenu RÉEL (`listItemsRecorded≥1, spritesMutated≥1 (=6), textsMutated≥1 (=4)` — tous à 0 avant le déblocage). **Prochain incrément à forte valeur : render-from-runtime** (composer le PNG depuis la sortie du driver — objets avec position + sprite-hash posés par le driver — au lieu de `build_sprite_list` statique). **SOUS-BLOQUEUR identifié + scopé (2026-06-16) : résolution des `spriteHash` du driver.** L'export runtime porte bien les sprite-hash par objet (`runtime.spriteHash`, ex. `0xA30165ED` banner avatar/équipe, `0xF5326827` dlc_logo) ; `objHash`=CRC32(nom d'objbin) résout (vérifié : `0xBA2C07D5`=`title02_02_item_atc_locator_2`), MAIS **`spriteHash` ne résout NI par basename de fichier NI par CRC32 des noms de régions des 6000 premiers g4tx de `/menu/`** (sur 40662). ⇒ le `spriteHash` réfère un nom de **sprite/région d'atlas interne** par un mécanisme à reverser (autre hash ? table de définition de sprites ? atlas hors `/menu/` type `220_img/` ?). render-from-runtime n'est donc PAS un incrément propre tant que ce mécanisme n'est pas reversé — scopé honnêtement pour éviter une tentative basename-naïve. **Brute-force d'index de noms ÉCARTÉ** : ni les 6000 1ers g4tx de `/menu/` ni un ciblage `icon_common`/`num_menu`/`title02`/`220_img` ne résolvent (ce dernier time-out — atlas trop volumineux à scanner). ⇒ la résolution `spriteHash→sprite` doit passer par la **RE de la fonction de résolution de sprite du moteur** (où le C++ mappe `texHash`→région d'atlas au rendu), pas par un index de noms forcé. CRC32 confirmé comme hash du système (objHash résout). Prochaine RE ciblée : la native/fonction moteur appelée au rendu d'un objet à `sprite_texture_hash`. **REVERSÉ (2026-06-16)** : le `spriteHash` runtime vient de `SetIconSprite` (cmdId `0x214DA123`, h1=clé-hash graphique) → `0x14053EB00`. Désassemblé : **`0x14053EB00` résout la clé-hash via un REGISTRE GLOBAL `[0x1421E3070]`** (lookup hash-map `0x140099870(registry, &hash)` → entrée graphique). Donc la résolution sprite = **registre global clé-hash→graphique**, peuplé à l'init depuis un **manifeste de noms de ressources graphiques** distinct des noms de régions g4tx (d'où l'échec du match région-g4tx). **Prochain incrément RE ciblé : identifier la source qui peuple le registre `[0x1421E3070]`** (manifeste/format de ressources graphiques de menu) → reconstruire l'index `nom-graphique-hash→texture` côté niers → débloque render-from-runtime. **Résolution par noms g4tx COMPLÈTEMENT ÉCARTÉE (vérifié 2026-06-16)** : les `spriteHash` (ex. `0xA30165ED` banner, `0xF5326827` dlc_logo) ne matchent NI basenames de fichiers, NI noms de régions des 6000 1ers g4tx `/menu/`, NI les noms des atlas `MENU_RES` EXACTS de title02 (`num_ver`/`num_menu01`/`logo_dlc_*`/`logo_title_*`, 105 noms). ⇒ 2 hypothèses restantes, toutes deux profondes : (a) les clés-hash réfèrent des noms d'un **manifeste de ressources graphiques distinct** (qui peuple `[0x1421E3070]`, à trouver) ; (b) le sous-système graphique utilise un **hash ≠ CRC32** (le système objet/layer EST CRC32 — objHash résout — mais les graphiques pourraient différer). Trancher exige la RE de la population de `[0x1421E3070]` (recherche de xref `r2` = time-out sur 32 Mo → nécessite `aa` complet, lourd) ou un couple nom→hash connu. **CRC32-de-nom ÉPUISÉ ET ÉCARTÉ (2026-06-16)** : 0 match sur **110 formes de chemins/noms** testées (basenames, noms de régions g4tx, noms d'atlas `MENU_RES`, chemins logiques `#/menu/...` avec/sans `.g4tx`/`<LG>`) — alors que le sanity-check confirme que CRC32 est le bon algo (objHash `title02_02_item_atc_locator_2`=`0xBA2C07D5` matche). **Puisque le système OBJET est CRC32 mais qu'AUCUNE forme de nom graphique ne matche, le sous-système graphique utilise très probablement un hash ≠ CRC32 (ou des constantes pré-hachées opaques).** ⇒ seule voie restante : reverser la population de `[0x1421E3070]`. Hypothèses CRC32-naïves toutes éliminées — évite toute future tentative perdue de ce type. **REGISTRE LOCALISÉ (2026-06-16, via `objdump -d` + grep des refs RIP-relatives — 356 refs)** : `[0x1421E3070]` est un **singleton STATIQUE** — getter `0x140046150` (`cmp [reg],0 ; lea rax,[0x141D86CE0] ; mov [reg],rax` = pointe vers l'objet statique `.data` **`0x141D86CE0`**, enregistre un destructeur atexit). La hash-map n'est donc PAS un manifeste statique mais **peuplée par des INSERTS au runtime (chargement de ressources)**. **Prochain trace ciblé : les call-sites d'INSERT de la hash-map** (sibling de la lookup `0x140099870`) → révèlent le NOM de ressource graphique + la FONCTION DE HASH appliquée. C'est là que se trouvent les vrais couples nom→hash. (Objet registre = `0x141D86CE0` ; ~356 refs au registre, surtout des lecteurs/consommateurs de graphiques.) **NOMS ABSENTS DU LUA — CONFIRMÉ (2026-06-16)** : extraction des constantes-chaînes du bytecode `title_menu_2.lua.bin` (98 chaînes distinctes) → les seules graphiques sont des noms de NŒUDS (`tex_banner_img01/02`, `mesh_banner_img01/02` — cibles de `SetNodeSprite`), PAS les noms de ressources sprite. Testés sous CRC32/FNV-1a/FNV-1 vs les `spriteHash` → 0 match. ⇒ **les `spriteHash` sont des constantes NUMÉRIQUES pré-calculées dans le bytecode (noms perdus à la source)**. TOUS les raccourcis sont désormais éliminés (noms g4tx, atlas MENU_RES, formes de chemins, chaînes Lua, 3 algos de hash). **Seule voie : reverser le LOADER de ressources** (le code qui insère les graphiques dans `[0x1421E3070]` au chargement avec `key=hash(nom)`) pour extraire à la fois la source des noms ET la fonction de hash. Effort RE profond multi-étapes, scopé sans ambiguïté. **Avancée trace (2026-06-16)** : les **99 refs à `0x140099870` sont toutes des LOOKUPS** (find), pas des inserts ; le cluster dense `0x140BB4660` est un **consommateur batch-lookup**, pas le producteur. ⇒ l'INSERT/population utilise une **méthode hash-map distincte** (non encore localisée) appelée au chargement de ressources. Prochain pas précis : trouver la fonction d'insert de la hash-map (sœur de la find `0x140099870`) et ses call-sites de chargement. **ÉLIMINATION DÉFINITIVE des raccourcis data-side (2026-06-16)** : **0 match sur 1518 noms de régions d'atlas candidats (num_*/logo_dlc/icon_common/title02/banner) × 5 algos de hash (CRC32, FNV-1a, FNV-1, djb2, CRC32-big-endian)**. Avec les éliminations précédentes (basenames, chemins, chaînes Lua), **PROUVÉ qu'AUCUN raccourci côté DONNÉES n'existe** : les noms de ressources sprite ne sont pas les noms de régions g4tx sous un hash standard. ⇒ la résolution `spriteHash→texture` EXIGE strictement la RE du loader/insert du registre `[0x1421E3070]` (extraire les vrais noms + la fonction de hash propriétaire). Tout futur essai data-side serait du temps perdu — conclusion verrouillée. **STRUCTURE HASH-MAP du registre reversée (2026-06-16)** : la find `0x140099870` est un wrapper **thread-safe** (spinlock `xchg [map+0x48]` + `SwitchToThread`) qui lit la clé u32 et appelle le **bucket-find réel `0x14009A080(map, &key, flag)`** ; layout map : array `[map+8]`, entrées de **24 octets** (constante `/3` `0x2AAA...AB`). L'insert est un find-or-insert via `0x14009A080`. ⇒ Trace ~6 fonctions de profondeur (SetIconSprite→0x14053EB00→0x140099870→0x14009A080 + registre statique). **Prochain pas précis : trouver le call-site d'INSERT qui ajoute un GRAPHIQUE (valeur, pas juste clé) → remonter la valeur graphique → la source du NOM + la fonction de hash.** Effort multi-session restant, mais chemin désormais entièrement balisé fonction par fonction. **Affinage trace (2026-06-16)** : `0x140099870` est une **FIND pure** (renvoie entrée-ou-null) → ses ~99 call-sites sont tous des LECTEURS. L'INSERT/population utilise une **fonction distincte** (find-or-create, sœur du bucket-find `0x14009A080`) NON encore localisée. ⇒ prochain pas : localiser la méthode INSERT de la classe hash-map (ou trouver les écritures dans l'array `[registre+8]`), puis le loader qui l'appelle avec un graphique → noms + hash. Note méta : la résolution sprite est un sous-projet RE multi-session ; chaque session descend 1-2 couches. Le déblocage `render-from-runtime` viendra de cette chaîne, mais d'autres incréments livrables (cmdId reversals, etc.) restent plus rentables par cycle. **🎯 RÉSOLU — toute la chaîne de RE-registre ci-dessus est SUPERSÉDÉE (2026-06-16, données VPS récupérées par l'utilisateur)** : `data/lua_scripts/` (677 Lua DÉCOMPILÉS + 1143 `.lua.bin` + `analysis/`) inclut **`crc32-dictionary.json` (160513 noms→hash, copié en `data/re/menu-crc32-dictionary.json`)**. **Les `spriteHash` SONT du CRC32 de noms** (vérifié : `CRC32("Focus")=0xA30165ED`, `0x35C8938D="offset01"`) → le dico les résout DIRECTEMENT (2988 noms graphiques `tex_`/`mesh_`/`icon`/`sprite`/`img`). **PAS besoin de reverser le registre/loader** : les noms viennent du corpus décompilé. Le Lua décompilé montre aussi le mécanisme : `SetIconSprite(objId, texturePathCrc, textureNameCrc, …)` = paire (chemin g4tx, nom de région), tous deux résolubles par le dico. ⇒ render-from-runtime est désormais DÉBLOQUÉ côté résolution. **CÂBLÉ (2026-06-16)** : `load_menu_crc32_dict()` (nie-game) charge le dico, l'export runtime résout `spritePath`/`spriteName` (ex. title02 `my_team_banner→"Focus"` 3/6 ; shop 4/5). **+ `SetIconSprite` capture désormais LA PAIRE** (chemin g4tx arg1 + région arg2 → `sprite_texture_hash`/`sprite_region_hash` dans `MenuObjectState`) → l'export résout `spriteRegion` en vrais noms de région (ex. shop `gtxt_rarity01_05`/`gtxt_rarity01_10`). **Reste render-from-runtime** : index `nom-de-région → fichier g4tx` (quel g4tx contient `gtxt_rarity01_05`) → charger la région → composer au transform de l'objet. Foundation posée, pipeline texture à finir. **+ Le Lua décompilé permet de reverser les cmdId restants par leurs SITES D'APPEL** (args nommés + contexte) au lieu du désassemblage nie.exe — bien plus rapide. Cf. retrieval VPS `ssh vps` (OVH 51.77.147.152). Cf. les 12 cmdId déjà portés. Le « 0 objet muté » se raffine en « 9 onglets énumérés ; reste : rendu des onglets + item-buttons (`GetItemButtonNum`/état C++) + porter cmdId `0x65E825B1` ». **🖼️ PIXELS PRODUITS — chaînon final FERMÉ (2026-06-16)** : la signature RÉELLE de `SetIconSprite` (relue sur les sites d'appel Lua) est `(objId, CRC32(chemin_g4tx_COMPLET), regionName, CRC32(region_dummy), 0, layer)` → **arg1 est le chemin g4tx VFS complet** (ex. `#/menu/200_icon/05_icon_rarity/<LG>/icon_rarity.g4tx`), résolu DIRECTEMENT par le dico (round-trip vérifié) — **aucun index nom-région→fichier à construire**. `nie-formats::g4tx` : `find_sub_texture`/`G4tx::region`/`region_rect` (nom de région → texture porteuse + rect (x,y,w,h), testé). `nie-game` : `crop_rgba` (rogne un rect RGBA, 2 tests) + diagnostics `--g4tx-regions` (liste les régions réelles) et `--g4tx-region <nom> --capture <png>` (décode la DDS + rogne → PNG). **VALIDÉ END-TO-END SUR DONNÉES RÉELLES** : `gtxt_rarity01_05` → `data/dx11/menu/200_icon/05_icon_rarity/fr/icon_rarity.g4tx` → texture `icon_rarity` 1604×1052 DDS → crop (0,264 / 800×128) → **bandeau « JOUEUR LÉGENDAIRE » RÉEL** (60183/102400 px opaques, vérifié visuellement). Gates `icon_rarity_atlas_regions_resolve` + `icon_rarity_region_crops_to_real_pixels` (menu_render_gate **9→11**), nie-formats +1, nie-game bin +2, clippy 0. **La chaîne « commande runtime → pixels exacts » est désormais COMPLÈTE et prouvée pour un sprite.** Reste : composer ces crops au transform de l'objet (placement D1.a) + UV des maillages, pour les intégrer au compositeur d'écran complet. **INDEX `région → g4tx` CONSTRUIT + CÂBLÉ DANS L'EXPORT (2026-06-16)** : sur les écrans réels (shop/title02), le runtime fournit un `spriteRegion` réel (`gtxt_rarity01_05`) MAIS un `spritePath` qui est un nom de NŒUD (`Focus`/`offset01`, via SetNodeSprite), pas un chemin g4tx — d'où le chaînon `région → fichier g4tx` manquant. Résolu : `data/re/menu-icon-atlases.txt` (24 atlas d'icônes extraits des sites d'appel SetIconSprite du corpus Lua) + cmd `--build-region-index` → `data/re/menu-region-index.json` (**410 régions, 17/24 atlas, gtxt_rarity01_05 → icon_rarity.g4tx**). Câblé dans `cmd_export_layout_runtime` (loader `load_region_index` + cache g4tx) : chaque objet à `spriteRegion` émet désormais `spriteRegionG4tx` + **`spriteRect {x,y,w,h}`** (g4tx chargé, `region_rect`). Validé : `shop` → `shop01_17_trade_condition` rect `(0,264,800,128)` + `shop01_30_basara_list_base` rect `(804,792,800,128)`, tous deux `icon_rarity.g4tx` ; `runtimeSummary.regionRects=2`. Gate `shop_runtime_resolves_region_to_g4tx_rect` (menu_render_gate **11→12**). **Le layout JSON exporté est désormais RENDER-READY pour ces régions** (le compositeur natif OU azalee web peut rogner + placer). **🎬 IMAGE COMPOSÉE — render-from-runtime BOUCLÉ pour un sprite (2026-06-16)** : primitives `scale_nearest` (resampling NN) + `blit_over` (straight alpha-over, clippé) ajoutées (4 tests unitaires) ; mode `--compose-layout <json> --capture <png>` : pour chaque objet VISIBLE à `spriteRegionG4tx`+`spriteRect`+`transform`, charge le g4tx, décode la texture porteuse, rogne la région, l'échelonne (`scaleX/Y`) et la pose au `transform` (ancre `anchorX/Y`) sur un canevas 1280×720. **VALIDÉ VISUELLEMENT** : `shop` → le bandeau de rareté `gtxt_rarity01_10` (« BASARA », holographique) est rogné de `icon_rarity.g4tx` et composé centré (x=640,y=360, 59262 px opaques). Gate `shop_runtime_composes_to_image` (export→compose chaînés, menu_render_gate **12→13**). **La chaîne ENTIÈRE « commande Lua runtime → image composée correcte » fonctionne pour un sprite.** **🖥️ ÉCRAN COMPLET COMPOSÉ (2026-06-16)** : `--compose-layout` dessine désormais AUSSI les **sprites statiques** de base (chaque objet à `sprite.logicalPath` → `select_main_texture` → décode → pose au transform), **z-ordonnés par `drawPriority`** (fond d'abord), avec les régions runtime par-dessus. Sur `shop` : **18 sprites composés (17 statiques + 1 région runtime)**, 907520 px opaques → un écran de boutique RECONNAISSABLE (fond dégradé + 5 barres de liste colorées + le bandeau « BASARA » sur sa barre). Gate `shop_runtime_composes_to_image` renforcé (PNG d'écran complet > 250 Ko, vs ~80 Ko pour 1 sprite — verrouille que la couche statique s'est dessinée). C'est le passage de « 1 sprite rendu » à « **un écran entier composé depuis le layout runtime** ». **Modèle de placement VÉRIFIÉ conforme au GPU** : le forward-map CPU (`coin local → (local−ancre)·échelle → +position`) est IDENTIQUE à `build_sprite_quad` (le quad du compositeur wgpu validé SSIM) — placement cohérent avec le chemin pixel-perfect, pas une approximation. **Finding main_menu (2026-06-16, honnête)** : `--menu main_menu --runtime --from-setting --compose-layout` ne rend QUE 7 sprites de fond/guide (dégradé + barres) — les **9 onglets d'en-tête (le CONTENU réel) n'ont ni sprite ni région résolus** (objets virtuels créés par le driver, sans sprite objbin) → image dominée par une barre de liste, bien pire que la voie compositeur existante (SSIM 0,418). **Ce n'est PAS un bug du compositeur** mais le gap connu « rendu des onglets » (résoudre `obj_hash`→objbin→texture + placement `SetupHeaderTab`). Donc le compose runtime est bon pour les écrans dont les sprites résolvent (shop) ; les écrans à objets virtuels (onglets main_menu) attendent la résolution sprite des onglets. Reste pour le pixel-perfect (SSIM ≥ 0,99) : **résolution sprite des onglets virtuels** (main_menu), les sprites par-nœud (`SetNodeSprite`), les UV/régions des sprites statiques (vs texture entière), le texte, le placement des item-buttons dynamiques (`SetupItemButton`), et la rotation (`rot`, ignorée par le blit axis-aligned actuel). **📊 MESURE SSIM de la voie compose-runtime (2026-06-16)** : `main_menu` composé via `--runtime --from-setting --export-layout` → `--compose-layout` → SSIM vs `menu.png` = **0,4059** — **À PARITÉ avec le compositeur statique validé** (`mainmenu_via_setting_ssim` = 0,418 ; la bande `header_tab` à l'échelle-fallback coûte ~0,012). ⇒ le chemin render-from-runtime est **quantitativement sain** sur la métrique du plan, pas seulement « ça ressemble à un écran ». Gate `mainmenu_runtime_compose_ssim` (plancher 0,30 ; menu_render_gate **13→14**). **Diagnostic data-driven (mesuré ce cycle)** : `rot != 0` = **0** sur shop/title02/main_menu (rotation non utilisée → blit axis-aligned suffisant) ; sprites par-nœud non-g4tx = **1 seul** (title02 `offset01`) → ces deux gaps sont marginaux. Le vrai bloqueur SSIM restant est le **transform du driver** (échelle/position réelles des widgets, ex. `header_tab` 5280×520 à l'échelle 1.0 fallback ; `mainmenu01_10/11` hors écran y>720) + le **rendu de texte** + la **résolution sprite des onglets** — tous dépendants de l'émulation du driver C++/Lua (machine d'état G4RA / `Setup*`), confirmée multi-session.
- **D1.c-driver — couverture funcLua : RE déterministe + dérive binaire (2026-06-19)** : **découverte structurante** — le `nie_eacpatched.exe` courant du VPS (3 juin 2026) est un **build DISTINCT** de celui des reversals documentés : le cmdId SetIconSprite `0x214DA123` (occurrence UNIQUE) est à l'entrée de table `0x141A85350`/handler `0x140CA3210` (doc = `0x141BDFD90`/`0x140CE74D0`). Les **cmdId sont stables** (hash de nom) mais **toutes les VAs de handler/table glissent** → désormais désassembler à la table COURANTE. Tooling ré-ancré + recouvert : `scripts/extract_funclua_table.py` réécrit (robuste au build, 3471 entrées du binaire courant) ; **`data/re/` régénéré localement** (gitignoré, perdu — pas de distant : cette machine EST le VPS 51.77.147.152) via `extract_funclua_table.py` + nouveau `extract_menu_icon_atlases.py` (11 atlas) + `nie-game --build-region-index` (527 régions) + copies (funclua-cmdids iecode, crc32-dict). **Nouveau RE déterministe** : `scripts/triage_funclua_handlers.py` (iced-x86 + bornes `.pdata` à **chunks chaînés UNW_FLAG_CHAININFO** + détection fiable de la valeur de retour + **analyse flux-de-contrôle de la garde d'arité**) classe les handlers non-portés. Classe `RETURN_1_SAFE` = retours ⊆ {0,1} avec 1, et tout `al=0` soit **absent** soit **confiné au bloc d'échec de la garde** (`cmp edx,N ; jae MAIN ; …xor al,al;ret`) — bloc mort à l'exécution (scripts livrés passent ≥N args). **38 cmdId portés** (`ARG_GUARDED_RETURN1` dans `menu_host.rs`, 4 lots par fréquence décroissante 287→8 ; tous CF-vérifiés `RETURN_1_SAFE`, spot-checks `0x86544EF0`/`0x85179093` confirment le pattern garde ; return 1.0, champ non modélisé) → **couverture du volume d'appels funcLuaMenuCommand 75,5 %→82,1 %** (61→115 cmdId portés), nie-lua **35 tests** verts, clippy 0. *(Rendements décroissants : le résidu est setters à floats, getters, `ARG_GUARD_TRUE` à 0-hors-garde, et longue traîne freq<8 — chaque cmdId ≈ 0,05 % de couverture.)* Toolbox RE Python installée (`.venv` : capstone/pefile/lief/iced-x86/unicorn/keystone/r2pipe + maturin/ruff). Reste backlog scopé par le triage : 6 `ARG_GUARD_TRUE` (`al=0` HORS garde = vrai chemin d'échec → vérif manuelle requise), 7 setters à floats, 1 getter.
- **D1 (historique) — bump GPU** : bump **wgpu 22→29.0.3** (+ winit 0.30.13, pollster 0.4 — migration vérifiée point par point dans `STACK.md`) et **retarget du port D3D11** `nie-engine/src/render.rs` (`FUN_14045ab10`/`c780`/`459110`/`459210`) vers `wgpu::Surface` + le pipeline plein écran déjà écrit dans `nie-game`. *Gate pixel-diff à deux étages : **égalité octet** (sha2/blake3 du RGBA8 dé-paddé) PUIS **SSIM ≥ 0,99 / PSNR** (image-compare) vs capture du jeu sur scènes-test menu.*
- **D-audio** : `cpal 0.18.1` en **pur transport**, mixeur **CRI Atom Ex maison** (`nie-engine/src/audio.rs`), PCM depuis `cridecoder`. *Gate : PCM bit-identique à CRI.*
- **D-vidéo** : `media-codec-vpx 0.8` (**VP9**, le décodeur du jeu) — **CORRIGER** l'étiquetage H.264 faux (`cri_audio.rs` / `cartographie-data.md` ; `nie.exe` n'a **aucun** chemin H.264). Prérequis : déchiffrement Level-5 des USM avant `usm_demux`. *Gate : YUV→RGBA via la matrice CRI Mana.*
- **D-lua** : **VM Lua réelle** via crate `nie-lua` (`mlua =0.11.6`, `lua52`+`vendored` = PUC-Rio 5.2.4, la VM exacte du jeu) en remplacement du simulateur de dispatch de `scripting.rs`. *Gate : scripts gameplay déterministes identiques.*
- **D2** : skinning g4sk + animations → modèles animés (**port maison f32 scalaire + glam `scalar-math`**, pas d'ozz). *Gate : pose identique sur frame-test.*
- **D3** : scène de match rendue. *Gate : SSIM sur séquence de match.*

### Pilier E — RE / échafaudage (C5) → nommer, pas seulement classer
- **E0 (FAIT)** : 92,45 % classé sur `.pdata`, graphe d'appels réel, RTTI (1 575 classes).
- **E1 (FAIT 2026-06-10)** : **arêtes indirectes** (`lea reg,[fn]` + slots vtable `.rdata`↔RTTI) → connecter le résidu isolé. *Gate : delta de couverture mesuré honnêtement, sans double-comptage.*
- **E2 (AMORCÉ 2026-06-10)** : **nommage**. Fait : noms **structurels** `Namespace::Classe::vmethod_N` écrits sur les méthodes de vtable des classes RTTI localisées → **6 429 fonctions nommées (12,18 %)**, 0 → non nul (name_source='vtable-struct', distincts des symboles originaux). Reste : ré-exporter un index Ghidra **aligné** (`analyzeHeadless`, dispo sur le VPS) pour récupérer les **vrais symboles** C++ (name_source='ghidra'/'pdb'). *Gate atteint (named > 0) ; gate suivant : N fonctions à nom sémantique réel.*
- **E3** : ré-ingestion du nommage comme ancres → boucle vers pilier C.

## 4. Trajectoire (vagues) et pilotage de fond

La campagne avance par **vagues** ; chaque vague = sélectionner des cibles bornées à vérité terrain →
orchestrer agents code + recherche → **vérifier (build + clippy -D warnings + tests/diff)** → mesurer → vague suivante.

**Vagues réalisées (2026-06-10)** :
- **Vague 1 — FAIT** : B1 (chara-param + aura-cmd clos, golden réel) + recherche A2/A5/E1 (clé HCA récupérée, CPK résolu, design arêtes indirectes).
- **Vague 2 — FAIT** : A2 (HCA décode réellement) ; A1 (nxtch N/A pour le PC) ; A5 (enveloppe CPK = pas un verrou).
- **Vague 3 — FAIT** : E1 (arêtes indirectes, 92,45 → 93,36 %) → heartbeat de fond réinjecté ; E2 amorcé (nommage structurel, 0 → 6 429).
- **Vague 4 — FAIT** : C1 (boucle de match jouable déterministe, 167 tests).
- **Vague 6 — FAIT** : match piloté par les **vraies données** — `with_formation` (placements byte-exacts) + `from_chara_params_and_levels` (stats réelles via croissance, mapping position tranché par iecode) ; +3 familles C2 (formation, command, ai), 4→7/58.

**Vagues suivantes** :
- **Vague 7 (cap actuel)** : **D1 natif** — bump wgpu 29 + retarget `render.rs` + **premier rendu d'une scène menu avec gate pixel-diff (SSIM + égalité octet)** ; puis B2 (familles `team`/`soccer`/`party`/`phase` → enrichir le match) ; C2-logique (résorber les EXTERN par sous-système) ; E2 complet (ré-export Ghidra aligné → vrais symboles).
- **Vague 8+** : longue traîne — **D2 skinning g4sk** (port maison + glam scalaire) puis **D3 scène de match rendue** ; D-lua (mlua) / D-audio (cpal) / D-vidéo (libvpx VP9) ; B2 (familles restantes), modèle de but/PRNG réels (RE), nettoyage décodeur HCA déprécié.

**Heartbeat RE de fond** (`var/re-heartbeat.log`, PID dans `var/re-heartbeat.pid`) : rejoue le vrai pipeline
`.pdata→vtable[+ancrage+nommage]→disasm[+lea]→propagate` sur binary_id=2 et logue la couverture réelle ;
amplifie automatiquement tout nouveau lever de code au cycle suivant. (L'ancienne boucle `automl-loop`
suivait le mauvais binaire id=1 à 88 % — remplacée.)

## 5. Métriques de progression (tableau de bord honnête)

À mettre à jour à chaque vague (source = mesure réelle, jamais déclaration) :

```
                       baseline →  2026-06-10        →  2026-06-19                       cible
C1 fichiers lisibles : 84,06 %  →  84,06 % +audio✓   →  ~99,99 % (par extension ; +DXBC/PXCL/g4nv/g4cm/g4mt/g4ma byte-validés)  100 %
C2 familles données  : 4/58     →  31/58 (réel byte) →  saturé vs inagle (+ bug nfc corrigé + audit motif-frère)  58/58
funcLua D1.c (D1)    : —         →  —                →  82,1 % du volume d'appels (115 cmdId, CF-vérifiés)  ~100 %
C3 fn logique portées: ~55      →  ~56 +CRand byte   →  +constantes init match ; modèle-de-but LOCALISÉ (anchors courants) ; runtime bloqué sur ORACLE (differential-testing)  sous-systèmes fonctionnels
C4 rendu            : —         →  — (host en place) →  texte D1.d FAIT (rendu+compositeur, gate) ; reste driver/skinning/scène = multi-session  ≥0,99 SSIM
C5 classé            : 92,45 %   →  93,36 %           →  93,36 % (pas de RE échafaudage cette session)  croissant
C5 nommé             : 0         →  6 429 (12,18 %)   →  6 429 (inchangé)                 croissant
```
> **Verrous structurels restants (clarifiés 2026-06-19, ≠ « juste long »)** : C3-runtime = **absence d'oracle** (les algos runtime — détection but/arrêt/score/physique — n'ont ni C décompilé à vecteurs réf, ni inagle, ni captures → reversables mais non *validables-FAIT* sans le jeu qui tourne ; sortie = harnais **differential-testing**, infra faisable ici mais multi-session). C4 = **driver D1.c** (résolution sprite/texte des onglets + positions) + skinning g4sk + scène GPU. Les deux sont des **projets de tooling/RE multi-session**, pas des incréments d'un tour.

> **Notes de reconciliation (2026-06-13)** :
> - **C1** : la métrique officielle reste **84,06 %** (2026-06-10) ; `docs/cartographie-data.md` mesure **92,6 %** après l'ajout de p3lip (20 357 fichiers lip-sync) — à reconcilier en métrique officielle, sans effacer le 84,06 %.
> - **C1 — couverture PAR EXTENSION (mesure 2026-06-19)** : sur les **250 800** fichiers du VFS (40 extensions), **246 001 = 98,09 %** ont un **parseur typé dédié** dans `nie-formats` (+1 428 ce cycle : g4nv/g4cm/g4mt) ; +1 027 (fxbin/ptlb) lisibles via `cfgbin` T2B → **98,50 %**. ⚠ « parseur existe pour l'extension » ≠ « parse prouvé sur tout le corpus » (validés byte ce cycle sur le corpus RÉEL : g4nv 156/156, g4cm 1210/1210, g4mt 62/63, g4ma 35/35, **DXBC vfxo+pfxo 2448/2448**, fxbin/ptlb via `/cfg`). **Mise à jour après DXBC + fxbin/ptlb + PXCL (2026-06-19)** : couverture conteneur/en-tête par extension ≈ **99,99 %** (~250 780/250 800). **TOUS les formats significatifs sont couverts** : Level-5 (g4*), cfg.bin/T2B (incl. fxbin/ptlb + **clobin/linb** confirmés ce cycle : `DA_BONE_LINE_START`/`LINE_EFF`), audio CRI (HCA/ADX/AWB/ACB/USM), **DXBC** (vfxo/pfxo 2448/2448 + **cfxo/gfxo** confirmés DXBC ce cycle), **PXCL** (col 1143/1143). **Reste** : ~20 fichiers (g4tg 9, r41* 8, sans-extension) — bas-volume, non bloquant. ⚠ « conteneur/en-tête parsé » : les **intérieurs opaques de format tiers** (bytecode shader SM5 dans DXBC-SHEX ; maillage PhysX-cooked dans PXCL) ne sont pas décodés (domaine SDK Microsoft/PhysX, hors RE Level-5) — mais ces assets ne sont pas sur le chemin du gameplay/rendu pixel-perfect. **C1 est essentiellement BOUCLÉ au niveau lecture d'asset.**
> - **C4** : le **host natif `nie-game`** est **en place** (2026-06-13, rend une vraie `.g4tx`, capture PNG bit-exacte). **Premier écran de menu composé de bout en bout (2026-06-14)** : `nie-game --menu title00` → 15 sprites réels (objbin+g4pkm+g4tx → placement byte-exact → blit CPU) → l'écran d'avertissement santé du jeu, reconnaissable. Reste : pipeline GPU + **gate SSIM** (nécessite une capture de référence du vrai jeu) — aucune mesure SSIM encore (pas de prétention pixel-perfect).

## 6. Honnêteté (le cadre, pas une excuse)

Reverser puis réécrire 100 % d'un jeu AAA de 31 Mo est un **effort de longue haleine** assumé. « 100 %
pixel-perfect » est la **direction**, pas une livraison de session. Ce roadmap garantit que **chaque pas est
réel et mesuré** : un format qui parse le vrai fichier, une donnée recalculée au bit, une fonction portée
validée contre le décompilé, un pixel diffé contre le jeu. La distinction *classifié ≠ nommé ≠ compris ≠
porté ≠ validé* est tenue partout — c'est elle qui empêche de confondre 92 % de cartographie avec 92 % de jeu.
