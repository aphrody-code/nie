{
  "mcpServers": {
    "niers-game": {
      "type": "stdio",
      "command": "bun",
      "args": ["run", "${NIERS_REPO}/apps/nie-mcp/src/index.ts"],
      "env": {
        "NIERS_REPO": "${NIERS_REPO}",
        "NIERS_REDIS": "redis://127.0.0.1:6379",
        "MODEL_SERVE_URL": "http://127.0.0.1:8790"
      }
    }
  }
}
