// Re-export des types Supabase générés (depuis types.gen.ts).
// Le fichier types.gen.ts est généré par `bun run types:gen` puis commit.

export type {
	Database,
	Tables,
	TablesInsert as Insert,
	TablesUpdate as Update,
	Enums,
	Json,
} from "./types.gen";
