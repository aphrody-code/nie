{
	"name": "@nie/bridge",
	"version": "1.0.0",
	"private": true,
	"description": "Control bridge contract and client for the native Rust nie-mcp server and the Inacord page of nie web.",
	"type": "module",
	"main": "src/index.ts",
	"module": "src/index.ts",
	"types": "src/index.ts",
	"exports": {
		".": {
			"import": "./src/index.ts",
			"types": "./src/index.ts"
		}
	},
	"license": "MIT",
	"engines": {
		"bun": ">=1.3.0"
	},
	"scripts": {
		"test": "bun test",
		"typecheck": "bunx tsc --noEmit"
	},
	"devDependencies": {
		"typescript": "catalog:",
		"@types/bun": "catalog:"
	}
}
