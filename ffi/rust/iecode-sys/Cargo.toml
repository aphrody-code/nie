[package]
name = "iecode-sys"
version = "1.0.0"
edition = "2024"
description = "Raw FFI bindings to libiecode — Level-5/Inazuma Eleven Victory Road RE toolkit"
license = "MIT"
links = "iecode"

[build-dependencies]
# No bindgen — hand-written bindings for stability

[features]
default = []
static-link = []

[[example]]
name = "basic"
